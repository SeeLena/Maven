jQuery(document).ready(function() {
    jQuery('.banner').addClass("hidden").viewportChecker({
        classToAdd: 'text-focus-in',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.offer').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.gallery-carousel').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.statistic').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.app').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.partnering').addClass("hidden").viewportChecker({
        classToAdd: 'text-focus-in',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.comments').addClass("hidden").viewportChecker({
        classToAdd: 'text-focus-in',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.featured').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.foundation').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.awards').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.get-started').addClass("hidden").viewportChecker({
        classToAdd: 'slide-in-left',
        offset: 400,
        classToRemove: 'hidden'
    });

    jQuery('.app__item').addClass("hidden").viewportChecker({
        classToAdd: 'flip-in-ver-right',
        offset: 400,
        classToRemove: 'hidden'
    });
    
});